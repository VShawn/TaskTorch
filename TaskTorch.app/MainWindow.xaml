<Window x:Class="TaskTorch.app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskTorch.app"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <ListBox ItemsSource="{Binding FileList}" Grid.Row="1" Height="300" Margin="0 5 0 0">
                <ListBox.ItemTemplate >
                    <DataTemplate >
                        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <Border Margin="5,1,5,1" Padding="3" BorderBrush="Green"  BorderThickness="1" CornerRadius="2">
                                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="300" ></ColumnDefinition>
                                        <ColumnDefinition Width="Auto" ></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <Label Content="{Binding Name}"></Label>
                                    <!--<Button Content="Open" Width="75" HorizontalAlignment="Left" Grid.Column="1" 
                                            Click="BtnListBoxSelect_OnClick" ToolTip="{Binding Name}"
                                    />-->
                                </Grid>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Orientation="Horizontal">
                <Button Name="BtnNewTask" Click="BtnNewTask_OnClick">New</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
